{
    "name": "bank-webhook",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "npx tsc -b && node dist/index.js",
        "build": "npx tsc -b"
    },
    "dependencies": {
        "@repo/db": "*",
        "express": "^4.18.3"
    },
    "devDependencies": {
        "@types/express": "^4.17.21",
        "@types/node": "^20.11.24",
        "typescript": "^5.4.0"
    }
}